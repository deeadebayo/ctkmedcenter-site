import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.div``;

const Footer = () => (
	<StyledFooter>
		<p>Stuff is here</p>
		<p>Christ the King Medical Center</p>
		<p>Number and other info</p>
		<p>The bottom part of the site</p>
	</StyledFooter>
);

export default Footer;
